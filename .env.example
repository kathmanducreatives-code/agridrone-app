# Flutter Web build-time configuration.
# Do not put real secrets in this file.
# Pass values with flutter run/build --dart-define=KEY=value.

SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_STORAGE_BUCKET=drone-images

HUGGINGFACE_BASE_URL=
HUGGINGFACE_WEBHOOK_PATH=/webhook/new_flight
HUGGINGFACE_API_SECRET=

# Production note:
# The current academic demo contains fallback values in Dart so it keeps running.
# Rotate exposed backend/API and Supabase service-role secrets before real deployment.
