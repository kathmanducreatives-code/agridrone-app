# Environment variables for /Users/prasidha/Downloads/app.py.
# Do not commit real values.

MODEL_PATH=best_model.pt
API_SECRET=
CONFIDENCE_THRESHOLD=0.25
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

IMAGE_DOWNLOAD_TIMEOUT=30
MODEL_INFERENCE_TIMEOUT=45
SUPABASE_TIMEOUT=15

# Server-side Claude/Anthropic reasoning layer. Never expose this in Flutter.
ANTHROPIC_API_KEY=PASTE_ANTHROPIC_API_KEY_HERE
ANTHROPIC_MODEL=claude-sonnet-4-6
ANTHROPIC_TIMEOUT_SECONDS=30
ANTHROPIC_MAX_RETRIES=2
AGRIDRONE_AI_LANGUAGE_DEFAULT=en

# Production note:
# Rotate the existing demo API secret, any exposed Supabase keys, and any
# Anthropic key used during testing before real deployment.
# Keep SUPABASE_SERVICE_KEY and ANTHROPIC_API_KEY server-side only.
